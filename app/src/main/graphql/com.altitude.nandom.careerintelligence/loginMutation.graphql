mutation LoginMutation(
    $email: String!,
    $password: String!
)
{
   loginCandidate (
     email: $email
     password: $password
   )
   {
      jwt
      email
    name{
      last
    }
   }
  }